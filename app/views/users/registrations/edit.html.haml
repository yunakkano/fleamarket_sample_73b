= render 'items/header_breadcrumbs'
- breadcrumb :email_password
= render 'layouts/breadcrumbs'
%main.mypage__container.clearfix
  =render partial: 'users/sidemenu', locals: {active_menu: "メール/パスワード"}
  =render 'users/registrations/userinfo_edit'

= render 'items/appbanner'
-# footerの部分テンプレート呼び出し
= render 'items/footer'
= render 'items/camerabutton'